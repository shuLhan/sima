app.title	=Sistem Informasi Manajemen Aset
app.name	=sima
app.extension	=.php
app.path	=/
app.module.dir	=module
app.paging.size	=50
app.media.dir	=media

;;
;; app.content.type values:
;; - 0 (single): default, one single module displayed in content area
;; - 1 (tabbed): module displayed per tab
;;
app.content.type=1

;;
;; app.menu.mode values
;; - 0	: tabbed toolbar mode
;; - 1	: dropdown menu mode
;;
app.menu.mode	=0

;; MySQL
;db.url		="mysql:host=localhost;dbname=jaring"

;; SQlite
db.url		=sqlite:/db/jaring.sq3

db.username	=jaring
db.password	=jaring
db.pool.min	=10
db.pool.max	=100
